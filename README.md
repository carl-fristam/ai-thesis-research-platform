# M4 Fullstack Starter 🚀

A lightweight Fullstack boilerplate built on an M4 Mac. This project features a **Python (Flask)** backend and a **React (Vite)** frontend, designed to run entirely in user-space without requiring admin privileges.

## 🏗 Project Structure

- **/backend**: Python Flask API with its own virtual environment (`venv`).
- **/frontend**: React + Vite frontend using Node.js.
- **start_all.sh**: A helper script to launch both servers simultaneously.

---

## 🛠 Prerequisites

- **Python 3.x**
- **Node.js** (Managed via NVM recommended)
- **Git**

---

## 🚀 Getting Started

### 1. Clone the Repository
```bash
git clone [https://github.com/carl-fristam/m4-fullstack-test.git](https://github.com/carl-fristam/m4-fullstack-test.git)
cd m4-fullstack-test
